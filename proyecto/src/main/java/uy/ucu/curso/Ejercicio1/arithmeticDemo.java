package uy.ucu.curso.Ejercicio1;

public class arithmeticDemo { 
    public static void main(String[] args) {
     int result = 1 + 2;// Aca conviene poner 3 de una, osea una asignacion simple.
        result = result - 1; //Aca una asignacion compuesta, porque se esta usando el mismo resultado para hacer la operacion.
          result = result * 2;// 
           result = result / 2;
            result = result + 8; 
            result = result % 7; 
        }

        String texto = "123";
        int numero = Integer.parseInt(texto);

        

         } 
